# 商談価格ナビ_タイヤ_アルミ統合版 Ver1.6.4

## 使い方

1. `index.html` をブラウザで開きます。
2. 「管理」から夏タイヤ、冬タイヤ、BSアルミ、社外アルミの価格表を読み込みます。
3. 「タイヤ」「アルミホイール」でボタン選択式の検索を行い、「セット見積に選択」を押します。
4. 「セット見積」で合計金額を確認し、必要に応じて印刷します。
5. 工賃、廃タイヤ、チューブレスバルブ、ナット代、その他費用は「セット見積」で含める/含めないと金額を調整できます。画像DB管理は「管理」で行います。

## Ver1.1.0の主な変更

- タイヤ価格表とアルミ価格表を別々に保持するように修正しました。
- アルミ検索にPCD検索を追加し、候補は `4/100` や `5/114.3` のように表示します。
- タイヤインチに応じたインチ別工賃を自動反映します。
- タイヤ＋アルミのセット品は組替工賃にセット品割引率を反映します。初期値は30%です。
- ナット代は、選択中アルミの穴数 × 330円 × 4枚を初期値にします。
- 印刷タイトルを「お見積書」に変更しました。
- 印刷項目を管理画面で選択できるようにしました。
- 印刷用レイアウトは見積書部分のみを対象にしました。

## Ver1.2.0の主な変更

- 夏タイヤ、冬タイヤ、BSアルミ、社外アルミの4価格表を別々に保持します。
- タイヤ検索は夏/冬を個別または両方で検索できます。初期状態は両方ONです。
- アルミ検索はBS/社外を個別または両方で検索できます。初期状態は両方ONです。
- 検索結果カードに価格表ラベルを表示します。
- 印刷CSSを見積書部分だけ印刷する設定に再調整しました。

## Ver1.3.0の主な変更

- アルミ掛率の初期値を0.90にしました。
- 定価が取得できるアルミは、定価×アルミ掛率で販売価格を計算します。
- BSアルミ価格表の「55掛」などの仕切り欄表示を読み取り、管理画面だけに表示します。
- BSアルミ価格表から読み込む商品はメーカー名をBSで統一します。

## Ver1.4.0の主な変更

- アルミホイール検索にインチ選択を追加しました。
- アルミ販売価格を標準で「仕切価格÷アルミ掛率」で計算します。初期値は0.90です。
- 管理画面でアルミ計算方法を「仕切価格÷掛率 / 仕切価格×掛率 / 販売価格をそのまま使用」から選べるようにしました。
- アルミ掛率変更後、価格表を編集せず検索結果とセット見積へ反映します。
- お客様向け画面の読み込みステータス欄を非表示にしました。
- タイヤ検索に「乗用タイヤ / OEM / VAN」区分を追加しました。
- 乗用タイヤとOEMのサイズ表示はロードインデックス・速度記号を外し、VANタイヤはLI表記を残すようにしました。
- アルミ検索UIを「メーカー → ブランド → パターン → インチ → PCD → サイズ」に整理しました。
- アルミのインチ候補は14〜20インチに絞り、インチ選択後にそのインチのサイズだけを表示するようにしました。
- アルミのサイズ選択ボタンは「インチ×リム幅」の大分類だけを表示するようにしました。
- お客様向け検索画面から穴数検索とカラー検索を外しました。
- 管理画面に価格表・画像DBの読込状態、ファイル名、読込日時、読込件数を表示するようにしました。
- BSアルミ価格表の仕切り表示をブランド別に確認できるようにしました。

## Ver1.5.0の主な変更

- BSアルミの検索階層をメーカーBS固定 → ブランド → パターンに整理しました。
- BSアルミのブランドを `BALMINUM / ECO FORME / TOPRUN` などに自動分離します。
- BSアルミのパターンはブランド選択後、そのブランド配下のみ表示します。
- アルミカード、セット見積、印刷でブランド＋パターンが分かる表示にしました。

## Ver1.6.0の主な変更

- 印刷画面のアルミカードからPCD・インセットの重複表示を外しました。
- 見積書の商品名サイズを落ち着いた大きさに調整しました。
- 管理画面にアルミ画像表示のON/OFF設定を追加しました。初期値はOFFです。

## Ver1.6.1の主な変更

- 管理画面を基本設定、価格設定、工賃設定、表示設定、画像管理のアコーディオン表示に整理しました。
- 画像管理表示のON/OFF設定を追加しました。初期値はOFFです。
- 画像管理表示OFF時は、WHEEL IMAGE DBや画像登録・画像一覧などの画像管理UIを非表示にします。

## Ver1.6.2の主な変更

- TOPRUN / ECO FORME / BALMINUM のアルミブランド別値引き率を管理画面で設定できるようにしました。初期値は8%です。
- 対象ブランドの見積価格は仕入価格 ×（100 - 値引率）% で計算します。
- タイヤ検索・アルミ検索の検索項目表示順を管理画面で上下移動できるようにしました。
- `npm run build` 用のビルド設定を追加し、静的配布用のdistを生成できるようにしました。

## Ver1.6.3の主な変更

- iPad Safari / ホーム画面追加PWAで白紙印刷になりにくいよう、印刷ボタンから印刷専用HTMLを生成して印刷する方式に変更しました。
- 既存の見積書デザインを維持しつつ、`visibility:hidden` 方式の印刷CSS依存を外しました。

## Ver1.6.4の主な変更

- iPad Safari / PWAでは非表示iframe印刷を使わず、印刷専用画面を表示してから印刷する方式に変更しました。
- 印刷後に元画面へ戻れる操作ボタンを追加しました。

## 画像DB運用

- 画像DB: `data/wheel_image_master.json`
- 画像フォルダ: `wheel_images/`
- ブラウザだけではフォルダへ直接保存できない場合があるため、画像登録後に推奨ファイル名で画像がダウンロードされます。
- ダウンロードした画像を `wheel_images/` に入れ、書き出した `wheel_image_master.json` を `data/` に入れてください。

## 統合方針

- タイヤ価格表解析は既存のタイヤ商談価格ナビ Ver2.6.0 の専用解析ロジックを移植しています。
- アルミ価格表解析は既存アルミホイール価格ナビ同梱のExcel読込ライブラリを使い、TAW形式、BS価格体系表、BSカタログ非掲載品に対応しています。
- データはブラウザ内に保持され、再読み込み後も可能な範囲で復元します。
- PWAとしてホーム画面追加、オフラインキャッシュに対応しています。
